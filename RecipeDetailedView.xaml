<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CookNook.RecipeDetailedView"
             Title="{Binding RecipeName}"
             xmlns:model="clr-namespace:CookNook.Model"
             BackgroundColor="#202c39">

    

    <ContentPage.ToolbarItems>
        <!-- SHOULD THIS BE EDIT, NOT ADD?-->
        <!--top-right corner button (plus button) -->
        <ToolbarItem Order="Primary" IconImageSource="plus.png" />
    </ContentPage.ToolbarItems>

    <CollectionView Margin="10">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:Recipe">
                <Frame BackgroundColor="LightGray" Padding="10" CornerRadius="10" Margin="5">
                    <Grid RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="200" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Recipe Name -->
                        <Label Text="{Binding Name}" Grid.Row="0" FontSize="Medium" FontAttributes="Bold"/>

                        <!-- Recipe Description -->
                        <Label Text="{Binding Description}" Grid.Row="1"/>

                        <!-- Recipe Image -->
                        <Image Source="{Binding Image, Converter={StaticResource ByteArrayToImageSourceConverter}}" Grid.Row="2"/>

                        <!-- Cook Time -->
                        <Label Text="{Binding CookTime}" Grid.Row="3"/>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>

<!--ADD INGREDIENTS HERE-->

<!-- Recipe Instructions -->

<!--ADD FOLLOWER COUNT HERE-->
<!--ADD TAGS HERE-->
<!--ADD COURSE, SERVINGS, RATING, OTHER DETAILS HERE-->

<!-- Recipe Description -->
<!--ADD AUTHOR HERE-->
<!-- Recipe Name and Image -->