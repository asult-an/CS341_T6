<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converter="clr-namespace:CookNook.Model"
             x:Class="CookNook.Cookbook"
             Title="{Binding PageTitle}"
             NavigationPage.HasBackButton="False"
             BackgroundColor="#202c39">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:ByteToImageConverter x:Key="ByteToImageConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <!--TODO: Use a picker to bind to a collection of CookbookPageModels-->
    <!--
    <Picker>
        The dropdown will display page_title, but we still need to track the list_id
        <Picker.ItemDisplayBinding>
            <Binding Path="PageTitle"/>
    </Picker>-->
    
    <CollectionView x:Name="recipesCollectionView" Margin="10">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BackgroundColor="LightGray" Padding="10" CornerRadius="10" Margin="5">
                    <Grid RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /> 
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="200" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Recipe Name -->
                        <Label Text="{Binding Name}" Grid.Row="0" FontSize="Medium" FontAttributes="Bold"/>

                        <!-- Recipe Description -->
                        <Label Text="{Binding Description}" Grid.Row="1"/>

                        <!-- Recipe Image -->
                        <Image Source="{Binding Image, Converter={StaticResource ByteToImageConverter}}" Grid.Row="2" Aspect="AspectFill"/>

                        <!-- Cook Time -->
                        <Label Text="{Binding CookTime}" Grid.Row="3"/>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>


